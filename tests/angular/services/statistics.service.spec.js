ngDescribe({
    name: 'Test StatisticsService',
    modules: 'app',
    inject: 'StatisticsService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
